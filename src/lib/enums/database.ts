export enum DBTypes {
  VarChar = 'varchar',
  Text = 'text',
  Enum = 'enum',
  Geometry = 'geometry',
  BigInt = 'bigint',
  TinyInt = 'smallint',
  Int = 'integer',
  Decimal = 'decimal',
  UUID = 'uuid',
  Float = 'float',
  JSON = 'json',
  Boolean = 'boolean',
  DateTime = 'timestamp'
}

export enum OrderWay {
  ASC = 'ASC',
  DESC = 'DESC'
}
